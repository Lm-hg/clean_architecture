name: CI - deploy on prod 

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  php-lint-and-test:
    name: PHP Linting & Testing
    runs-on: ubuntu-latest
    
    services:
      postgres:
        image: postgres:14-alpine
        env:
          POSTGRES_USER: parking_user
          POSTGRES_PASSWORD: test_password
          POSTGRES_DB: parking_db_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
      
      mongodb:
        image: mongo:7.0
        env:
          MONGO_INITDB_ROOT_USERNAME: parking_mongo
          MONGO_INITDB_ROOT_PASSWORD: test_password
          MONGO_INITDB_DATABASE: parking_db_test
        ports:
          - 27017:27017
        options: >-
          --health-cmd "mongosh --eval 'db.runCommand({ ping: 1 })'"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.3'
          extensions: pdo, pdo_pgsql, zip, gd, mongodb
          coverage: none
          tools: composer:v2

      - name: Validate composer.json
        run: composer validate --strict

      - name: Cache Composer dependencies
        uses: actions/cache@v3
        with:
          path: vendor
          key: ${{ runner.os }}-composer-${{ hashFiles('**/composer.lock') }}
          restore-keys: |
            ${{ runner.os }}-composer-

      - name: Install dependencies
        run: composer install --prefer-dist --no-progress --no-interaction

      - name: Check PHP syntax errors
        run: find src -name "*.php" -print0 | xargs -0 -n1 php -l

      - name: Run PHPUnit tests
        env:
          DB_HOST: localhost
          DB_NAME: parking_db_test
          DB_USER: parking_user
          DB_PASSWORD: test_password
          MONGO_HOST: localhost
          MONGO_DB: parking_db_test
          MONGO_USER: parking_mongo
          MONGO_PASSWORD: test_password
        run: |
          if [ -f "vendor/bin/phpunit" ]; then
            vendor/bin/phpunit --testdox
          else
            echo "PHPUnit not configured yet, skipping tests"
          fi

  docker-build:
    name: Docker Build Verification
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Create .env file for testing
        run: |
          cat > .env << EOF
          POSTGRES_USER=parking_user
          POSTGRES_PASSWORD=test_password
          POSTGRES_DB=parking_db_test
          DB_HOST=db
          DB_NAME=parking_db_test
          DB_USER=parking_user
          DB_PASSWORD=test_password
          MONGO_USER=parking_mongo
          MONGO_PASSWORD=test_password
          MONGO_DB=parking_db_test
          EOF

      - name: Build Docker images
        run: docker compose build --no-cache

      - name: Start Docker containers
        run: docker compose up -d

      - name: Wait for services to be ready
        run: |
          echo "Waiting for PostgreSQL..."
          timeout 60 bash -c 'until docker compose exec -T db pg_isready -U parking_user; do sleep 2; done'
          echo "Waiting for MongoDB..."
          timeout 60 bash -c 'until docker compose exec -T mongodb mongosh --eval "db.runCommand({ ping: 1 })" --quiet; do sleep 2; done'
          echo "Waiting for web server..."
          sleep 10

      - name: Check container health
        run: |
          docker compose ps
          docker compose logs web
          docker compose logs db
          docker compose logs mongodb

      - name: Test health endpoint
        run: |
          response=$(curl -s -o /dev/null -w "%{http_code}" http://localhost/health)
          if [ "$response" != "200" ]; then
            echo "Health check failed with status: $response"
            docker compose logs web
            exit 1
          fi
          echo "Health check passed!"

      - name: Test PostgreSQL connection
        run: |
          response=$(curl -s http://localhost/db-test)
          echo "$response"
          if echo "$response" | grep -q '"status":"success"'; then
            echo "PostgreSQL connection test passed!"
          else
            echo "PostgreSQL connection test failed!"
            exit 1
          fi

      - name: Test MongoDB connection
        run: |
          response=$(curl -s http://localhost/mongo-test)
          echo "$response"
          if echo "$response" | grep -q '"status":"success"'; then
            echo "MongoDB connection test passed!"
          else
            echo "MongoDB connection test failed!"
            exit 1
          fi

      - name: Cleanup
        if: always()
        run: docker compose down -v

  code-quality:
    name: Code Quality Checks
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.3'
          extensions: pdo, pdo_pgsql, zip, gd, mongodb
          coverage: none
          tools: composer:v2, phpstan, php-cs-fixer

      - name: Install dependencies
        run: composer install --prefer-dist --no-progress --no-interaction

      - name: Check code style (PSR-12)
        run: |
          if [ -f ".php-cs-fixer.php" ]; then
            php-cs-fixer fix --dry-run --diff --verbose
          else
            echo "PHP-CS-Fixer config not found, skipping"
          fi
        continue-on-error: true

      - name: Run PHPStan static analysis
        run: |
          if [ -f "phpstan.neon" ]; then
            phpstan analyse src --level=5
          else
            echo "PHPStan config not found, skipping"
          fi
        continue-on-error: true

  security-check:
    name: Security Vulnerability Check
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.3'
          tools: composer:v2

      - name: Check for security vulnerabilities
        run: composer audit
        continue-on-error: true
